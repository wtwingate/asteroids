[project]
name = "asteroids"
version = "0.1.0"
description = "Clone of the classic arcade game built with Pygame"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pygame==2.6.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.7",
]
